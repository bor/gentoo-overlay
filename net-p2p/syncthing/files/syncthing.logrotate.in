# logrotate snippet for syncthing
# copy the file into /etc/logrotate.d
/var/log/syncthing/syncthing.log {
    rotate 4
    missingok
    notifempty
    sharedscripts
    postrotate
        @RESTART_CMD@ > /dev/null 2>&1 || true
    endscript
}
